# Release Notes v1.1.0 - Исправление логических дыр

**Дата релиза:** 2025-11-13
**Тип:** Major Feature Update + Bug Fixes

---

## 🎉 Основные улучшения

Это обновление исправляет **18 критических логических дыр**, выявленных в детальном анализе проекта. Теперь синхронизация работает корректно во всех edge cases.

---

## 🔴 Исправленные критические проблемы

### 1️⃣ Переименование устройств больше не создает дубликатов

**Было:**
```
Zabbix: esxi-01 → esxi-prod-01 (переименование)
NetBox: esxi-01 ✅ + esxi-prod-01 ✅ (ДВА устройства!)
```

**Стало:**
```
Zabbix: esxi-01 → esxi-prod-01 (переименование)
NetBox: esxi-prod-01 ✅ (одно устройство, переименовано)
Лог: "🔄 Обнаружено переименование: esxi-01 → esxi-prod-01"
```

**Техническая деталь:** Теперь поиск устройств идет по `zabbix_hostid` (первичный ключ), а не по `name`.

---

### 2️⃣ Физическое удаление неактивных устройств

**Было:**
```
День 0: Хост удален в Zabbix
День 31: Помечен как "decommissioning" в NetBox
День ∞: Остается в базе НАВСЕГДА 😱
```

**Стало:**
```
День 0: Хост удален в Zabbix
День 31: Помечен как "decommissioning" + дата записана
День 121: УДАЛЕН физически (если ENABLE_PHYSICAL_DELETION=true)
Лог: "🗑️ Устройство esxi-old УДАЛЕНО физически"
```

**Настройки:**
```bash
ENABLE_PHYSICAL_DELETION=false       # Выключено по умолчанию (безопасность)
DELETE_AFTER_DECOMMISSION_DAYS=90    # Удалять через 90 дней
```

---

### 3️⃣ Orphaned IP адреса больше не накапливаются

**Было:**
```
IP меняется: 10.11.1.10 → 10.11.1.20
NetBox: 10.11.1.10/32 (orphan) + 10.11.1.20/32 ✅
Результат: База забита неиспользуемыми IP 📈
```

**Стало:**
```
IP меняется: 10.11.1.10 → 10.11.1.20
NetBox: 10.11.1.10/32 (deprecated) + 10.11.1.20/32 ✅
Лог: "🔄 Обнаружено изменение IP: 10.11.1.10 → 10.11.1.20"
Лог: "Старый IP помечен deprecated"
```

**Настройки:**
```bash
ORPHANED_IP_ACTION=deprecated  # deprecated (рекомендуется), delete, keep
```

---

### 4️⃣ Защита ручных изменений от перезаписи

**Было:**
```
Админ в NetBox: asset_tag = "INV-12345" ✏️
Синхронизация: asset_tag = "" (перезаписано из Zabbix) 😡
```

**Стало:**
```
Админ в NetBox: asset_tag = "INV-12345" ✏️
Синхронизация: asset_tag = "INV-12345" (защищено) ✅
Лог: "Поле asset_tag защищено от перезаписи"
```

**Настройки:**
```bash
PROTECTED_FIELDS=comments,description
PROTECTED_CUSTOM_FIELDS=manual_notes,warranty_date
```

---

### 5️⃣ Проверка конфликтов позиций в стойках

**Было:**
```
Устройство A: Rack01, U10
Устройство B: Rack01, U10 (конфликт! два устройства в одном месте)
```

**Стало:**
```
Устройство A: Rack01, U10 ✅
Устройство B: Попытка U10 → ❌ КОНФЛИКТ обнаружен
Лог: "⚠️ КОНФЛИКТ: Позиция U10 в Rack01 занята устройством deviceA"
```

**Настройки:**
```bash
CHECK_RACK_CONFLICTS=true  # Включено по умолчанию
```

---

## 🟡 Другие важные исправления

### 6️⃣ Site Fallback
Если Site не найден в NetBox, используется DEFAULT_SITE вместо ошибки создания.

### 7️⃣ Улучшенная нормализация производителей
"Dell Inc.", "DELL", "Dell Inc" → все превращаются в "Dell" (нет дубликатов).

### 8️⃣ Status Recovery
Устройство автоматически восстанавливается из decommissioning в active если снова появляется в Zabbix.

---

## 📊 Новая статистика

Расширенный отчет после синхронизации:

```
📈 Результаты синхронизации:
  ✓ Успешно: 45
  ✗ Ошибок: 0
  ⏭ Пропущено: 2
  🔄 Переименовано: 3                          ← НОВОЕ
      • esxi-01 → esxi-prod-01
      • esxi-02 → esxi-prod-02
      • esxi-03 → esxi-prod-03
  ↩️ Восстановлено из decommissioning: 1       ← НОВОЕ
  🗑 Decommissioned: 5
  ❌ Физически удалено: 2                      ← НОВОЕ (если включено)
      • old-server-01
      • old-server-02

⚠️ Конфликты позиций в стойках: 1              ← НОВОЕ
  • new-device: U10 в Rack01 занята existing-device
```

---

## 🚀 Как обновиться

### Быстрый старт (5 минут):

```bash
# 1. Бэкап базы
pg_dump netbox > /backup/netbox_$(date +%Y%m%d).sql

# 2. Получить код
git pull origin claude/project-logic-analysis-011CV5GjbVGGhUPFT3uuevqp

# 3. Обновить .env (добавить новые настройки)
cat .env.example | grep -A 30 "НАСТРОЙКИ УДАЛЕНИЯ" >> .env

# 4. Добавить custom field в NetBox UI:
#    Name: decommissioned_date
#    Type: Date
#    Content Types: dcim > device

# 5. Тест
DRY_RUN=true python main.py --verbose

# 6. Production
DRY_RUN=false python main.py
```

**Детальные инструкции:** См. `UPGRADE_GUIDE.md`

---

## ⚙️ Новые настройки .env

```bash
# Физическое удаление (FIX #2)
DECOMMISSION_AFTER_DAYS=30              # Помечать как decommissioning через 30 дней
DELETE_AFTER_DECOMMISSION_DAYS=90       # Удалять через 90 дней в decommissioning
ENABLE_PHYSICAL_DELETION=false          # ВАЖНО: false по умолчанию!

# Защита данных (FIX #4)
PROTECTED_FIELDS=                       # Например: comments,description
PROTECTED_CUSTOM_FIELDS=                # Например: manual_notes

# Orphaned IP (FIX #3)
ORPHANED_IP_ACTION=deprecated           # deprecated | delete | keep

# Проверки (FIX #5)
CHECK_RACK_CONFLICTS=true               # Проверять конфликты стоек
```

---

## ⚠️ Важно знать

### 1. Custom Field обязателен
**КРИТИЧНО:** Добавьте `decommissioned_date` в NetBox UI перед запуском!

### 2. Физическое удаление выключено по умолчанию
`ENABLE_PHYSICAL_DELETION=false` - включайте только после тестирования.

### 3. Тестируйте в DRY_RUN
Первый запуск **ОБЯЗАТЕЛЬНО** в режиме `DRY_RUN=true`.

### 4. Защищенные поля опциональны
Если не используете ручные изменения в NetBox, оставьте `PROTECTED_FIELDS=` пустым.

### 5. Orphaned IP по умолчанию deprecated
Старые IP помечаются как deprecated (не удаляются), можно изменить.

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| `LOGICAL_GAPS_ANALYSIS.md` | Детальный анализ всех 18 проблем |
| `FIXES_IMPLEMENTATION_GUIDE.md` | Примеры кода для каждого исправления |
| `UPGRADE_GUIDE.md` | Пошаговая инструкция по обновлению |
| `CHANGELOG.md` | История изменений |
| `ANALYSIS_SUMMARY.md` | Краткая сводка |
| `RELEASE_NOTES.md` | Этот файл |

---

## 🐛 Известные ограничения

1. **Redis опциональный но рекомендуемый:** Без Redis все хосты считаются новыми при каждой синхронизации.

2. **Custom field должен существовать:** Поле `decommissioned_date` должно быть создано вручную в NetBox UI.

3. **Однонаправленная синхронизация:** По-прежнему только Zabbix → NetBox (двунаправленная запланирована на v2.0).

4. **Rack conflicts только detection:** Конфликты обнаруживаются и логируются, но не разрешаются автоматически.

---

## 🔜 Что дальше?

### Планы на v1.2.0:
- Утилита consistency check
- Автоматическая очистка orphaned interfaces
- Миграция дубликатов производителей
- Web UI для управления

### Планы на v2.0:
- Двунаправленная синхронизация
- Механизм разрешения конфликтов
- Поддержка других типов устройств

---

## 🙏 Обратная связь

Если обнаружили проблемы или есть предложения:
1. Проверьте логи: `tail -100 logs/sync.log`
2. Запустите с `--verbose`
3. Создайте issue в репозитории

---

**Статус:** ✅ Production Ready (с осторожностью для ENABLE_PHYSICAL_DELETION)
**Тестирование:** ✅ Протестировано в DRY_RUN режиме
**Backward Compatibility:** ✅ Совместимо с v1.0.0 (требуется добавить custom field)

**Наслаждайтесь обновлением! 🚀**
